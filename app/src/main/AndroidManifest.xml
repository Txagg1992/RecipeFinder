<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.curiousapps.pixfinder">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE"/>

    <application
        android:name=".data.AppController"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        android:usesCleartextTraffic="true">
        <activity android:name="com.curiousapps.pixfinder.activities.DetailActivity"
            android:label="@string/pix_detail"/>
        <activity android:name="com.curiousapps.pixfinder.activities.ShowPictureActivity"
            android:label="@string/pix_url"/>
        <activity android:name="com.curiousapps.pixfinder.activities.RecipeList"
            android:label="@string/search_pix"/>
        <activity android:name="com.curiousapps.pixfinder.activities.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>